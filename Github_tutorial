Github tutorial

Sækja Git fyrir mac eða windows og installa. https://git-scm.com/downloads

Í gegnum terminal/cmd farið inn i möppuna þar sem þið viljið hafa repository-ið okkar(ATVR á github)

Skrifið i terminal/cmd : 
	
	git clone https://github.com/johanngunnar/ATVR.git

Þá ætti repository-ið(ATVR) að vera komið inn, kemur örugglega user og pw fyrst.

Þá er hægt að fara inn i ATVR möppuna og opna .py skjölin í editor og edita eins og maður vill.

Þegar maður buinn að edita og save-a .py skjalið Þá skrifa í terminal/cmd(vera inn í ATVR möppunni): 

	git add file_name.py  (nafnið á file sem var verið að edita)

Síðan terminal/cmd: 

	git commit -m "Breytingar á file_name.py" 

(Fint að gera þetta -m annars kemur eitthvað leiðelgt commit promp sem ekki hægt að komast úr nema ýta á esc og skrifa í terminal/cmd:  :wq)

og svo til að keyra breytingar inn í GitHub, terminal/cmd: 

	git push (Gera þetta alltaf áður en maður byrjar að vinna)

þá ættu breytingarnar að sjást inn í github.

Næsti maður sem vill þá fá file-inn inn í repository á tölvunni sinni notar command(inn i ATVR möppunni):

	git pull

Nice að nota: 

	git status 

Inn á milli í skrefum her fyrir ofan til að sja statusinn á file-unum.

Commands:

git add
git commit -m ""
git push
git pull
git status

git add -A (add all)

:wq (loka commit glugga)





